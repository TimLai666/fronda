## 1. agent_contract 線（tool_exec.rs、tools.rs、mutation.rs、id_short.rs、audio_core、media_library）

- [ ] 1.1 [P] #342：依 design「#342 auto 模式永遠建立新 shared tracks」，`cmd_add_clips` all-omitted 分支改為永遠建立新 shared tracks（視覺一條新視訊軌、音訊含 linked partners 一條新音訊軌置底），顯式 trackIndex 路徑不動；`tools.rs` add_clips 描述同步 `07030d6f` 用語。先寫 e2e 測試（transplant 上游 `addClipsOmittingAudioTrackIndexAppendsBelowLinkedDialogue` 意圖：linked dialogue 後全略 index 加 music，斷言 dialogue 完好、music 在新軌）再改實作，實作 spec「add_clips auto mode always creates fresh shared tracks」。驗證：`cargo test -p fronda-agent-contract add_clips` 全綠。
- [ ] 1.2 [P] #307：依 design「#307 manage_tracks 以 trackId 定址」，manage_tracks 契約對齊 `d87faaea`，實作 spec「manage_tracks addresses tracks by stable trackId」——`mutation.rs` validate（trackId XOR index；remove 接受整數或物件選擇器）、`cmd_manage_tracks`（trackId 解析、越 zone reorder 硬錯誤、reorderedTracks/removedTracks 回執、刪舊 note）、`get_timeline` track 物件加 trackId、`id_universe` 納入 track ids、`SCALAR_ID_KEYS` 加 "trackId"、schema/描述/system-instruction 逐字對齊。先 transplant 上游測試（trackId 定址跨 reorder、越 zone 錯誤、回執）再實作；受影響既有測試逐一核對是行為對齊後更新。驗證：`cargo test -p fronda-agent-contract manage_tracks` 與 `id_short` 全綠。
- [ ] 1.3 [P] #274-followups：依 design「#274-followups detect_beats 契約」，detect_beats 契約修復，實作 spec「detect_beats rejects silent video up front」「detect_beats windowed calls report window-local bpm」「Beat cache invalidates when the source file changes」——`audio_core::beat_detector` 曝露純函式 `estimate_bpm`（60/median IBI，<2 beats 回 None，含單元測試）；`cmd_detect_beats` 加 has_audio 前置拒絕、窗內 bpm 重算、空分析/空窗 note、bpm/downbeats 省略語意、beat_cache (size,mtime) 標記（stat 失敗退回無標記）。驗證：`cargo test -p fronda-audio-core beat` 與 `cargo test -p fronda-agent-contract detect_beats` 全綠。
- [ ] 1.4 [P] #333+#338（agent_contract/media_library 半）：依 design「#333 / #338 匯入契約字串與查表」，`tools.rs` import_media 描述與 path 屬性字串逐字同步 `0ae452a0`（in-place、同步 ready、檔案留原位）並把 caf 加入格式清單；`tool_exec.rs` mime→ext 表與拒絕訊息加 caf；`media_library` SupportedExtensions::AUDIO 加 caf。實作 spec「import_media contract text matches in-place registration semantics」與「CAF audio files are importable」的本線半邊。先寫描述內容斷言測試（不含 "downloading"、含 caf 與 in-place 語意）再改字串。驗證：`cargo test -p fronda-agent-contract import` 與 `cargo test -p fronda-media-library` 全綠。

## 2. core_model 線（timeline.rs、media_manifest.rs）

- [ ] 2.1 [P] #338（core_model 半）：依 design「#333 / #338 匯入契約字串與查表」與「core_model serde 相容欄位（#294/#336/#330 切片）」的施工原則，`ClipType::from_extension` 加 caf、`content_type_for_extension` 加 caf → `audio/x-caf`，補表格測試，實作 spec「CAF audio files are importable」的本線半邊。驗證：`cargo test -p fronda-core-model extension` 全綠。
- [ ] 2.2 [P] #294 切片：依 design「core_model serde 相容欄位（#294/#336/#330 切片）」，`GenerationInput` 加 `target_language`（`rename="targetLanguage"`、`skip_serializing_if=Option::is_none`），Swift 形 round-trip 測試（present 保值、absent 不寫 key），實作 spec「GenerationInput preserves targetLanguage」。驗證：`cargo test -p fronda-core-model generation` 全綠。
- [ ] 2.3 [P] #336+#330 切片：依 design「core_model serde 相容欄位（#294/#336/#330 切片）」，先 `git show upstream/main:` 讀 Swift TextStyle 與 Background 的權威定義取得逐 key 鍵名，再擴充 `TextStyle`/`TextStyleWire`：isUnderlined/isStruckThrough/isOverlined（bool default false）、tracking/lineSpacing（Option）、fontCase（Option<String> 保留 rawValue）、border 寬、Background 九欄位結構；讀取相容 pre-#330 舊形（缺鍵取 default），寫出 v0.6.10 鍵集合；渲染讀取者不變（wire 轉換填舊欄位或相容 getter）。dual-write round-trip 測試逐鍵斷言 + 舊形 decode 測試，實作 spec「TextStyle preserves v0.6.10 styling fields」。驗證：`cargo test -p fronda-core-model text_style` 全綠。

## 3. 收尾

- [ ] 3.1 `cargo test --workspace` 全綠、`cargo check -p fronda-app-shell-gpui --features desktop-app --bin fronda` 通過；`specs/rust-rewrite/97-upstream-pr-audit.md` 補 2026-07-17 re-audit 節（47 單元 tier 表 + 決策清單）；CLAUDE.md porting table 增列本批與 audit 狀態更新。驗證：內容審查 + 測試輸出記錄。
