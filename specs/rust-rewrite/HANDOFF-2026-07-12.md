# Handoff — closed 2026-07-17 (macOS verification complete)

This handoff is **complete**. The 2026-07-17 session finished everything the
previous update left open. Full evidence lives in
`VERIFY-2026-07-17-macos-shell.md` (same directory).

## What was completed on 2026-07-17

1. **Spectra ingest** — the font-backend and native-menu requirements were
   merged into change `editor-shell-parity` (proposal, design, delta spec,
   tasks; `spectra analyze` clean of Critical/Warning, `spectra validate`
   passes). The delta spec's "Title bar menu bar" requirement and main-spec
   `EDT-009` were corrected: Swift `MainMenu.swift` has **no Window menu**
   (app menu/File/Edit/View/Help), and macOS never had a system menu.
2. **Font fix committed to the change** — `desktop-app` feature enables
   `gpui_platform/font-kit`; regression test
   `desktop_app_enables_macos_font_backend` (red→green verified earlier).
3. **Native macOS menu implemented** — `menu.rs` gained `menu_sections()`
   (Swift separator grouping; `all_menus()` now flattens it so the two can
   never diverge), Layout short labels, and macOS `cmd-` keystroke strings.
   New `native_menu.rs` translates the shared model into gpui `Menu`/
   `MenuItem` (Layout as a submenu), every item dispatching the shared
   `RunMenuAction`; `main.rs` registers it at boot on macOS after the
   keybindings (the keymap feeds the menu key equivalents);
   `bind_menu_shortcut_keys` now also binds on macOS (`cmd-` chords, plus
   `!input`-predicated display bindings for Cmd+A/C/V/X/Z). Structure tests
   in `native_menu.rs` (5) + `menu.rs` (28 total) are green.
4. **Three defects found by interactive verification, all fixed**:
   - Nothing was focused at boot → all menu items validated disabled and
     all shortcuts were dead. `open_main_window` now focuses the AppRoot.
   - The empty-timeline composite is a fully transparent PNG that hid the
     empty-state canvas; the canvas bounds now always paint under the frame.
   - `assets.rs` never embedded captions/music_note/eye_slash/speaker_slash
     SVGs (silent no-render on every platform); embedded + regression test
     `every_icon_on_disk_is_embedded`.
5. **Task 5.2 interactive pass done on macOS** — text visible, native menu
   groups + key equivalents (⌘N/⇧⌘I/⌘1/⌘C), Layout switching via menu AND
   ⌘1/⌘2 with exactly one dispatch per trigger, divider drag verified both
   directions incl. the preview-min-400 clamp, welcome card fully visible at
   1280×720, three rail icons, preview canvas bounds measurable, and the
   Swift baseline (`swift run`) compared side-by-side for all three layouts.
6. **Gates** — `cargo test --workspace`: 2916 passed / 0 failed;
   `cargo check -p fronda-app-shell-gpui --features desktop-app --bin fronda`
   passes; desktop-gated suites (menu/native_menu/assets) green.

## Remaining broader work (unchanged backlog)

- Pixel-level (not structural) shell parity against Swift is still open.
- PanelFocusRing needs pane-focus tracking; pane visibility/size persistence
  across launches remains deferred (Swift persists visibility, EDT-003).
- The upstream Rust-porting re-audit for `771b63e..f0f5b473` is pending.
- Initial pane sizes at small windows can leave Preview under 400 pt until
  the first drag re-clamps (Swift-parity behavior; cosmetic).
- App-menu title shows the process name (`fronda`) until a proper app bundle
  sets CFBundleName; the system auto-appends its own "Enter Full Screen"
  View item because ours is a custom-action no-op on all platforms.
- Dev seams `FRONDA_OPEN_EDITOR=1` / `FRONDA_WINDOW_SIZE=WxH` remain and
  should be reviewed before a release build.
